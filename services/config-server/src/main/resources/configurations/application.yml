eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka
name:
  value: maiita

spring:
  cloud:
    config:
      override-system-properties: false

management:
  tracing:
    sampling:
      probability: 1.0